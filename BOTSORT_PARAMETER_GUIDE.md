# BotSort 파라미터 가이드

## 개요
BotSort는 YOLO 검출 결과를 기반으로 객체 추적을 수행합니다. 각 파라미터는 추적의 안정성, 재인식률, ID 스위치 방지 등에 영향을 줍니다.

---

## 핵심 파라미터 설명

### 1. `track_buffer` (현재: 120)
**의미**: 가림 상황에서 트랙을 유지할 프레임 수

**동작**:
- 객체가 사라져도 이 프레임 수만큼 트랙을 유지
- 이 시간 동안 재매칭 시도

**조정 효과**:
- **높게 (120 → 150)**: 가림 대응 강화, 재인식률 ↑, ID 스위치 위험 ↑
- **낮게 (120 → 90)**: 메모리 사용 ↓, ID 스위치 위험 ↓, 재인식률 ↓

**권장**: 90-120 (3-4초 @30FPS)

---

### 2. `match_thresh` (현재: 0.3)
**의미**: 기존 트랙과 새 검출을 매칭하는 IoU/비용 임계값

**동작**:
- 이 값 이상이면 같은 객체로 간주하여 기존 트랙에 연결
- 낮을수록 더 관대하게 매칭

**조정 효과**:
- **높게 (0.3 → 0.4)**: 매칭 엄격, ID 안정성 ↑, 재인식률 ↓
- **낮게 (0.3 → 0.25)**: 매칭 관대, 재인식률 ↑, ID 스위치 위험 ↑

**권장**: 0.25-0.35 (재인식률과 안정성 균형)

---

### 3. `new_track_thresh` (현재: 0.4)
**의미**: 새 트랙을 생성하는 최소 신뢰도

**동작**:
- 기존 트랙과 매칭되지 않은 검출이 이 값 이상이면 새 트랙 생성
- 높을수록 새 트랙 생성 억제

**조정 효과**:
- **높게 (0.4 → 0.5)**: 새 트랙 생성 ↓, ID 안정성 ↑, 새 사람 인식 ↓
- **낮게 (0.4 → 0.3)**: 새 트랙 생성 ↑, 새 사람 인식 ↑, ID 생성량 ↑

**권장**: 0.3-0.4 (ID 생성량과 새 사람 인식 균형)

---

### 4. `appearance_thresh` (현재: 0.65)
**의미**: ReID(외형 기반) 매칭의 최소 유사도

**동작**:
- 외형 유사도가 이 값 이상이면 같은 사람으로 간주
- 높을수록 외형 매칭 엄격

**조정 효과**:
- **높게 (0.65 → 0.75)**: ID 스위치 방지 ↑, 재인식률 ↓ (외형 변화 시)
- **낮게 (0.65 → 0.55)**: 재인식률 ↑, ID 스위치 위험 ↑

**권장**: 0.6-0.7 (재인식률과 ID 스위치 방지 균형)

---

### 5. `proximity_thresh` (현재: 0.3)
**의미**: ReID를 고려할 최소 IoU (위치 근접도)

**동작**:
- 두 트랙의 IoU가 이 값 이상일 때만 ReID 고려
- 낮을수록 더 넓은 범위에서 ReID 적용

**조정 효과**:
- **높게 (0.3 → 0.4)**: ReID 정확도 ↑, 재인식 범위 ↓
- **낮게 (0.3 → 0.2)**: 재인식 범위 ↑, 잘못된 매칭 위험 ↑

**권장**: 0.25-0.35

---

### 6. `track_high_thresh` (현재: 0.2)
**의미**: 1단계 매칭 임계값 (높은 신뢰도 검출)

**동작**:
- 높은 신뢰도 검출을 기존 트랙과 매칭할 때 사용
- 낮을수록 더 많은 검출을 매칭 시도

**조정 효과**:
- **높게 (0.2 → 0.3)**: 매칭 엄격, 안정성 ↑, 재인식률 ↓
- **낮게 (0.2 → 0.15)**: 매칭 관대, 재인식률 ↑, ID 스위치 위험 ↑

**권장**: 0.15-0.25

---

### 7. `track_low_thresh` (현재: 0.05)
**의미**: 2단계 매칭 임계값 (낮은 신뢰도 검출)

**동작**:
- 1단계 매칭 실패 시 낮은 신뢰도 검출로 재시도
- 낮을수록 약한 검출도 매칭 시도

**조정 효과**:
- **높게 (0.05 → 0.1)**: 매칭 엄격, 안정성 ↑, 재인식률 ↓
- **낮게 (0.05 → 0.02)**: 재인식률 ↑, 잘못된 매칭 위험 ↑

**권장**: 0.03-0.08

---

## 겹치는 사람 문제 해결 전략

### 문제: 겹치는 사람에게 타겟이 이동

**원인**:
1. `match_thresh`가 너무 낮아서 가리는 사람과 매칭됨
2. `appearance_thresh`가 너무 낮아서 외형 차이를 구분 못함
3. `proximity_thresh`가 너무 낮아서 멀리 있는 사람도 ReID 고려

**해결 방법**:

#### 방법 1: 매칭 엄격화 (권장)
```yaml
match_thresh: 0.35  # 0.3 → 0.35 (기존 트랙과 더 엄격하게 매칭)
appearance_thresh: 0.7  # 0.65 → 0.7 (외형 매칭 엄격화)
proximity_thresh: 0.35  # 0.3 → 0.35 (ReID 고려 범위 축소)
```

#### 방법 2: 새 트랙 생성 억제
```yaml
new_track_thresh: 0.45  # 0.4 → 0.45 (새 트랙 생성 더 억제)
```

#### 방법 3: 1단계 매칭 강화
```yaml
track_high_thresh: 0.25  # 0.2 → 0.25 (높은 신뢰도 검출만 매칭)
```

---

## 파라미터 조정 우선순위

### 재인식률 우선 (가림 대응)
```yaml
track_buffer: 120
match_thresh: 0.25
appearance_thresh: 0.6
proximity_thresh: 0.25
track_low_thresh: 0.03
```

### ID 안정성 우선 (겹침 방지)
```yaml
track_buffer: 90
match_thresh: 0.35
appearance_thresh: 0.7
proximity_thresh: 0.35
new_track_thresh: 0.45
track_high_thresh: 0.25
```

### 균형 (권장)
```yaml
track_buffer: 120
match_thresh: 0.3
appearance_thresh: 0.65
proximity_thresh: 0.3
new_track_thresh: 0.4
track_high_thresh: 0.2
track_low_thresh: 0.05
```

---

## 테스트 방법

1. **ID 생성량 확인**: 영상에서 최대 ID 번호 확인
2. **타겟 변경 횟수**: 가림 상황에서 타겟이 바뀌는 횟수 확인
3. **재인식률**: 1초 가림 후 재인식 성공률 확인

각 파라미터를 0.05씩 조정하며 테스트하는 것을 권장합니다.

